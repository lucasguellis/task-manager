const styles = {
  modalBg:
    'fixed inset-0 z-0 bg-gray-500/75 transition-opacity bg-opacity-50 flex justify-center items-center',
  modalSizer:
    'content-center flex justify-center fixed inset-0 z-10 w-screen overflow-y-auto',
  modalPosition:
    'sm:flex justify-center content-center p-4 sm:items-center w-3/4 sm:p-0',
  modalContainer:
    'relative transform overflow-hidden rounded-lg bg-white shadow-xl transition-all sm:my-8 sm:w-full sm:max-w-lg break-words',
  modalBorder: 'bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4',
  modalFooter: 'bg-gray-50 px-4 py-3 flex flex-row-reverse',
  closeButton: 'mr-2 sm:mt-0 sm:w-auto ',
};

export default styles;
